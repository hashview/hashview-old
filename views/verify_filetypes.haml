!!!
%html
  %body
    .span15
      .container
        .col-md-12
          .row
            .page-header
              %h1
                Verify File Format
            .row
              .col-md-10.pull-left
                Hashview automatically detected the file format (raw list of hashes, pwdump, etc.). If you'd like to override the detected format type, do so below.
      %br
      %br
      .container
        .col-md-12
          .row
            %form{:class => "form-horizontal", :action => "/customers/upload/verify_filetype", :method => "post"}
              .form-group
                %label.control-label.col-xs-2{:for => ""} Detected Hash File Type:
                .col-xs-4
                  %select.form-control{:name => "filetype"}
                    - @filetypes.each do |type|
                      %option{:value => "#{type}", :name => "filetype", :type => "button"} #{type}
                  %input{:type => 'hidden', :name => 'hashid', :value=>"#{params[:hashid].to_s}"}
              .form-group
                .col-xs-offset-2.col-xs-10
                  %input{:type => 'hidden', :name => 'job_id', :value=>"#{params[:job_id].to_s}"}
                  %input{:type => 'hidden', :name => 'customer_id', :value=>"#{params[:customer_id].to_s}"}
                  %button.btn.btn-primary{:type => "submit"} Confirm
