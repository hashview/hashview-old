!!!
%html
  %body
    %p
    .span15
      %br
      .container
        .row
          .col-md-12
            .container
              .row
                .col-md-12
                  .page-header
                    %h1
                      Verify the file format
                .row
                  .col-md-10.pull-left
                    Hashview automatically detected the following file format(s), however there may be instances where this is wrong. Select the correct file type and click confirm.
                %br
                .row
                  %form{:class => "form-horizontal", :action => "/customers/upload/verify_filetype", :method => "post"}
                    .form-group
                      %label.control-label.col-xs-2{:for => ""} Detected Hash File Type: 
                      .col-xs-4
                        %select.form-control{:name => "filetype"}
                          - @filetypes.each do |type|
                            %option{:value => "#{type}", :name => "filetype", :type => "button"} #{type}
                        %input{:type => 'hidden', :name => 'hashid', :value=>"#{params[:hashid].to_s}"}
                    .form-group
                      .col-xs-offset-2.col-xs-10
                        %input{:type => 'hidden', :name => 'jobid', :value=>"#{params[:jobid].to_s}"}
                        %input{:type => 'hidden', :name => 'custid', :value=>"#{params[:custid].to_s}"}
                        %button.btn.btn-primary{:type => "submit"} Confirm
